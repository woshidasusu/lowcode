import BaseInitHandle from './base';

/**
 * 设置表单项的列表项，通常是一些下拉组件才需要
 */
export default class OptionsHandle extends BaseInitHandle {
  async exec() {
    this.log('exec() start, handle =', this.handle);
    // TODO
  }
}
